# TODO

- [ ] Add a “Download Resume” button (top-right next to System button) that links to a Google Drive URL.
- [ ] Add a larger resume button near the bottom section (within the page footer area) pointing to the same Drive URL.
- [ ] Update code in `components/SystemNav.jsx` (top-right button).
- [ ] Update code in `components/Portfolio.jsx` (bottom larger button).
- [ ] Run the app / verify links open correctly.

